#profile-edit
  = form_for [@profile.user, @profile] do |f|
    %fieldset
      = f.collection_check_boxes :ethnicity_ids, @ethnicities, :id, :ethnicity
    %fieldset
      = f.label :height
      = f.select :height, @height_options
    %fieldset.button
      = f.submit "Save"
